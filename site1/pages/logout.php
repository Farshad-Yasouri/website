<?php
// جلسهٔ کاربر را آغاز می‌کند.
session_start(); 
// تمام داده‌های ذخیره‌شده در جلسه را پاک می‌کند.
$_SESSION = []; 
// جلسهٔ کاربر را به‌طور کامل از بین می‌برد.
session_destroy();
// کاربر را به فایل index.html منتقل می‌کند.
header("Location: ../index.html");
// اجرای باقی‌ماندهٔ کد را متوقف می‌کن 
exit; 