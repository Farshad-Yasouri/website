// عنصر HTML دارای شناسه‌ی "main" را پیدا می‌کند و در متغیر main قرار می‌دهد.
// getElementById فقط یک عنصر را بر اساس مقدار id پیدا می‌کند.
const main = document.getElementById("main");

// عنصر مرکزی با شناسه‌ی "centerCircle" را پیدا می‌کند.
// این عنصر احتمالاً همان دایره‌ای است که با کلیک روی آن کارت‌ها باز یا بسته می‌شوند.
const centerCircle = document.getElementById("centerCircle");

// تمام عنصرهایی را که کلاس "starcube" دارند پیدا می‌کند.
// querySelectorAll یک NodeList برمی‌گرداند و با ... آن را به آرایه‌ی معمولی تبدیل می‌کنیم.
const cards = [...document.querySelectorAll(".starcube")];

// وضعیت باز یا بسته بودن منوی کارت‌ها را نگه می‌دارد.
// مقدار اولیه‌ی false یعنی کارت‌ها در ابتدا بسته هستند.
let opened = false;


// برای عنصر centerCircle یک رویداد کلیک تعریف می‌کند.
// هر بار که کاربر روی دایره‌ی مرکزی کلیک کند، کد داخل تابع اجرا می‌شود.
centerCircle.addEventListener("click", () => {

  // مقدار opened را برعکس می‌کند؛ false به true و true به false تبدیل می‌شود.
// این کار باعث می‌شود با هر کلیک، منو باز یا بسته شود.
  opened = !opened;

  // کلاس active را از تمام کارت‌ها حذف می‌کند.
// بنابراین هنگام باز یا بسته شدن منو، هیچ کارتی انتخاب‌شده باقی نمی‌ماند.
  cards.forEach(c => c.classList.remove("active"));

  // بررسی می‌کند که آیا وضعیت جدید منو باز است یا نه.
// اگر opened برابر true باشد، دستورات داخل این بلوک اجرا می‌شوند.
  if (opened) {

    // کلاس open را به عنصر main اضافه می‌کند.
// این کلاس معمولاً برای تغییر ظاهر منو با CSS، مانند چرخش یا نمایش کارت‌ها، استفاده می‌شود.
    main.classList.add("open");

    // تابع چیدمان کارت‌ها را اجرا می‌کند.
// این تابع موقعیت هر کارت را به صورت دایره‌ای محاسبه می‌کند.
    arrangeCards();

  // اگر opened برابر false باشد، یعنی منو باید بسته شود.
  } else {

    // کلاس open را از main حذف می‌کند.
// با حذف این کلاس، ظاهر بازشده‌ی منو به حالت بسته برمی‌گردد.
    main.classList.remove("open");

    // روی همه‌ی کارت‌ها حرکت می‌کند و موقعیت آن‌ها را به مرکز صفحه منتقل می‌کند.
    cards.forEach(c => {

      // مقدار left کارت را روی 50 درصد عرض والد قرار می‌دهد.
// یعنی سمت چپ کارت در مرکز افقی والد قرار می‌گیرد.
      c.style.left = "50%";

      // مقدار top کارت را روی 50 درصد ارتفاع والد قرار می‌دهد.
// یعنی بالای کارت در مرکز عمودی والد قرار می‌گیرد.
      c.style.top = "50%";
    });
  }
});


// تابعی برای قرار دادن کارت‌ها در اطراف مرکز تعریف می‌کند.
// این تابع زمانی اجرا می‌شود که منو باز شود.
function arrangeCards() {

  // مختصات افقی مرکز را بر حسب درصد مشخص می‌کند.
// مقدار 50 یعنی مرکز در 50 درصد عرض والد قرار دارد.
  const centerX = 50;

  // مختصات عمودی مرکز را بر حسب درصد مشخص می‌کند.
// مقدار 50 یعنی مرکز در 50 درصد ارتفاع والد قرار دارد.
  const centerY = 50;

  // فاصله‌ی کارت‌ها از مرکز را مشخص می‌کند.
// مقدار 35 یعنی کارت‌ها در فاصله‌ای معادل 35 درصد ابعاد والد قرار می‌گیرند.
  const radius = 35;

  // زاویه‌ی لازم برای فاصله‌ی مساوی بین کارت‌ها را محاسبه می‌کند.
// 2π یک دور کامل دایره است و تقسیم آن بر تعداد کارت‌ها، زاویه‌ی بین دو کارت مجاور را می‌دهد.
  const angleStep = (2 * Math.PI) / cards.length;


  // روی هر کارت حرکت می‌کند و index شماره‌ی آن کارت است.
// forEach برای اجرای یک تابع روی تک‌تک اعضای آرایه استفاده می‌شود.
  cards.forEach((card, index) => {

    // زاویه‌ی کارت فعلی را محاسبه می‌کند.
// index * angleStep کارت‌ها را به صورت منظم دور دایره پخش می‌کند و -π/2 باعث می‌شود کارت اول از بالا شروع شود.
    const angle = index * angleStep - Math.PI / 2;

    // مختصات افقی کارت را با استفاده از کسینوس محاسبه می‌کند.
// cos برای تعیین موقعیت کارت روی محور افقی دایره به کار می‌رود.
    const x = centerX + Math.cos(angle) * radius;

    // مختصات عمودی کارت را با استفاده از سینوس محاسبه می‌کند.
// sin برای تعیین موقعیت کارت روی محور عمودی دایره استفاده می‌شود.
    const y = centerY + Math.sin(angle) * radius;

    // موقعیت افقی کارت را به صورت درصدی تنظیم می‌کند.
// x + "%" باعث می‌شود مقدار محاسبه‌شده به شکل CSS مانند "85%" نوشته شود.
    card.style.left = x + "%";

    // موقعیت عمودی کارت را به صورت درصدی تنظیم می‌کند.
// y + "%" باعث می‌شود کارت در مختصات عمودی محاسبه‌شده قرار بگیرد.
    card.style.top = y + "%";
  });
}


// روی هر کارت یک رویداد کلیک ایجاد می‌کند.
// با کلیک روی هر کارت، تابع مربوط به همان کارت اجرا می‌شود.
cards.forEach(card => {

  // رویداد کلیک را به کارت اضافه می‌کند.
// پارامتر e اطلاعات مربوط به رویداد کلیک را در اختیار تابع می‌گذارد.
  card.addEventListener("click", (e) => {

    // از رسیدن رویداد کلیک به عناصر والد جلوگیری می‌کند.
// بدون این دستور، کلیک روی کارت ممکن است باعث اجرای رویداد کلیک centerCircle یا عناصر والد شود.
    e.stopPropagation();

    // اگر منو بسته باشد، اجرای تابع متوقف می‌شود.
// return باعث می‌شود وقتی opened برابر false است، کارت هیچ واکنشی نشان ندهد.
    if (!opened) return;

    // کلاس active را از تمام کارت‌ها حذف می‌کند.
// در نتیجه فقط یک کارت می‌تواند حالت فعال یا انتخاب‌شده داشته باشد.
    cards.forEach(c => c.classList.remove("active"));

    // کلاس active را فقط به کارت کلیک‌شده اضافه می‌کند.
// این کلاس می‌تواند در CSS برای تغییر رنگ، اندازه یا نمایش جزئیات کارت استفاده شود.
    card.classList.add("active");
  });
});
/*
توضیح اجزای مهم کد
const: برای تعریف متغیری استفاده می‌شود که دوباره نمی‌توان مقدار مرجع آن را تغییر داد. مثلاً main همیشه به همان عنصر HTML اشاره می‌کند.
let: برای متغیری استفاده می‌شود که مقدار آن ممکن است تغییر کند. در این کد مقدار opened بین true و false تغییر می‌کند.
document: نماینده‌ی صفحه‌ی HTML فعلی است و از طریق آن می‌توان عناصر صفحه را پیدا یا تغییر داد.
getElementById("..."): عنصری را پیدا می‌کند که مقدار id آن برابر با متن داخل پرانتز باشد. برای نمونه، "main" باید در HTML به شکل id="main" وجود داشته باشد.
querySelectorAll(".starcube"): همه‌ی عنصرهایی را پیدا می‌کند که کلاس starcube دارند. نقطه‌ی قبل از نام، نشان‌دهنده‌ی انتخاب عنصر بر اساس کلاس CSS است.
[...]: این ساختار عملگر spread نام دارد. در اینجا NodeList حاصل از querySelectorAll را به یک آرایه‌ی واقعی تبدیل می‌کند تا بتوان راحت‌تر از متدهایی مانند forEach استفاده کرد.
addEventListener: برای گوش دادن به رویدادهای مختلف مانند کلیک، حرکت ماوس یا فشردن کلید استفاده می‌شود. در اینجا رویداد موردنظر "click" است.
() => {}: یک تابع فلشی است. این تابع کوتاه‌تر از function () {} نوشته می‌شود و برای اجرای کد هنگام وقوع رویداد به کار رفته است.
classList.add("open"): کلاس open را به عنصر اضافه می‌کند. معمولاً در CSS برای تعریف حالت باز منو استفاده می‌شود.
classList.remove("active"): کلاس active را حذف می‌کند. این کار باعث می‌شود حالت فعال کارت از بین برود.
classList: مجموعه‌ای از کلاس‌های CSS مربوط به یک عنصر است. با آن می‌توان کلاس‌ها را اضافه، حذف یا بررسی کرد.
style.left و style.top: مستقیماً ویژگی‌های CSS مربوط به موقعیت افقی و عمودی عنصر را تغییر می‌دهند. برای اثرگذاری صحیح، معمولاً عنصر باید در CSS دارای position: absolute یا position: relative باشد.
50%: یعنی نصف عرض یا ارتفاع عنصر والد. وقتی left و top روی ۵۰ درصد تنظیم می‌شوند، نقطه‌ی ابتدایی کارت در مرکز قرار می‌گیرد؛ برای مرکز دقیق کارت معمولاً باید از transform: translate(-50%, -50%) هم استفاده شود.
Math.PI: مقدار تقریبی عدد π، یعنی 3.14159 است. یک دور کامل دایره برابر با 2 * Math.PI رادیان است.
Math.cos(angle): مقدار کسینوس یک زاویه را برمی‌گرداند و برای محاسبه‌ی موقعیت افقی روی دایره استفاده می‌شود.
Math.sin(angle): مقدار سینوس یک زاویه را برمی‌گرداند و برای محاسبه‌ی موقعیت عمودی روی دایره استفاده می‌شود.
stopPropagation(): از انتقال رویداد به عناصر والد جلوگیری می‌کند. در این کد کمک می‌کند کلیک روی کارت، رویدادهای عناصر بیرونی را به شکل ناخواسته فعال نکند.
forEach: یک تابع را برای تک‌تک اعضای آرایه اجرا می‌کند. در کد شما هم برای تغییر همه‌ی کارت‌ها و هم برای ثبت رویداد کلیک روی آن‌ها استفاده شده است.
index: شماره‌ی هر کارت در آرایه است و از صفر شروع می‌شود. برای کارت اول مقدار 0، برای کارت دوم مقدار 1 و به همین ترتیب خواهد بود.
e: مخفف معمول event است و اطلاعات مربوط به رویداد را نگه می‌دارد. در این کد از e.stopPropagation() برای متوقف کردن انتشار کلیک استفاده شده است.
active: کلاسی است که به کارت انتخاب‌شده داده می‌شود. ظاهر واقعی آن باید در CSS تعریف شود؛ مثلاً:
.starcube.active {
  transform: translate(-50%, -50%) scale(1.2);
  z-index: 10;
}
open: کلاسی است که هنگام باز بودن منو به main اضافه می‌شود. می‌توان از آن برای تغییر ظاهر کلی منو استفاده کرد:
#main.open {
  transform: scale(1.05);
}


*/
